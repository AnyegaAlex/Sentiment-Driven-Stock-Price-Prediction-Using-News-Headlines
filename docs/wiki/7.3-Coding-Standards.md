# 7.3 Coding Standards

**File:** `/wiki/7.3-Coding-Standards.md`

---

## Overview

This page defines the coding standards for the Tickflow project. Consistent code style improves readability, maintainability, and collaboration. All contributors are expected to follow these standards.

---

## Python Coding Standards

### Linting and Formatting Tools

| Tool | Purpose | Configuration |
|------|---------|---------------|
| **Black** | Code formatting | Line length: 88 |
| **flake8** | Code linting | Max line length: 88 |
| **isort** | Import sorting | Profile: black |
| **bandit** | Security linting | - |

### Configuration Files

**pyproject.toml (Black):**

```toml
[tool.black]
line-length = 88
target-version = ['py311', 'py312']

[tool.isort]
profile = "black"
line_length = 88
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
```

**.flake8:**

```ini
[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude = .venv, migrations, node_modules
max-complexity = 10
```

### Python Naming Conventions

| Element | Convention | Example |
|---------|------------|---------|
| **Classes** | PascalCase | `UserProfileView` |
| **Functions** | snake_case | `calculate_performance_metrics` |
| **Variables** | snake_case | `total_predictions` |
| **Constants** | UPPER_SNAKE | `MAX_RETRIES` |
| **Private Methods** | _leading_underscore | `_load_model` |
| **Dunder Methods** | __double_underscore__ | `__init__` |

### Python Code Examples

**Good:**

```python
# stocks/utils.py
def calculate_performance_metrics(queryset):
    """
    Calculate precision, recall, f1, confusion matrix from queryset.

    Args:
        queryset: QuerySet of Prediction objects

    Returns:
        dict: Performance metrics including accuracy, precision, recall, f1
    """
    if not SKLEARN_AVAILABLE:
        logger.warning("sklearn not available – returning empty metrics")
        return {
            'accuracy': 0,
            'precision': 0,
            'recall': 0,
            'f1': 0,
            'confusion_matrix': {'TP': 0, 'FP': 0, 'TN': 0, 'FN': 0}
        }

    resolved = queryset.filter(is_correct__isnull=False)

    if resolved.count() == 0:
        return {
            'accuracy': 0,
            'precision': 0,
            'recall': 0,
            'f1': 0,
            'confusion_matrix': {'TP': 0, 'FP': 0, 'TN': 0, 'FN': 0}
        }

    # Extract actual and predicted
    y_true = []
    y_pred = []

    for pred in resolved:
        if pred.actual_direction in ['up', 'down']:
            y_true.append(1 if pred.actual_direction == 'up' else 0)
            y_pred.append(1 if pred.predicted_movement == 'up' else 0)

    if len(y_true) == 0:
        return {
            'accuracy': 0,
            'precision': 0,
            'recall': 0,
            'f1': 0,
            'confusion_matrix': {'TP': 0, 'FP': 0, 'TN': 0, 'FN': 0}
        }

    tn, fp, fn, tp = confusion_matrix(y_true, y_pred, labels=[0, 1]).ravel()

    accuracy = (tp + tn) / (tp + tn + fp + fn) if (tp + tn + fp + fn) > 0 else 0
    precision = tp / (tp + fp) if (tp + fp) > 0 else 0
    recall = tp / (tp + fn) if (tp + fn) > 0 else 0
    f1 = 2 * (precision * recall) / (precision + recall) if (precision + recall) > 0 else 0

    return {
        'accuracy': round(accuracy * 100, 1),
        'precision': round(precision * 100, 1),
        'recall': round(recall * 100, 1),
        'f1': round(f1 * 100, 1),
        'confusion_matrix': {'TP': tp, 'FP': fp, 'TN': tn, 'FN': fn}
    }
```

**Bad:**

```python
def calc(q):
    if not SKLEARN_AVAILABLE:
        return {}
    r = q.filter(is_correct__isnull=False)
    if r.count() == 0:
        return {}
    yt = []
    yp = []
    for p in r:
        if p.actual_direction in ['up', 'down']:
            yt.append(1 if p.actual_direction == 'up' else 0)
            yp.append(1 if p.predicted_movement == 'up' else 0)
    # ... more poorly formatted code
```

### Python Import Order

1. **Standard library**
2. **Third-party packages**
3. **Django imports**
4. **Project imports**

**Example:**

```python
# Standard library
import logging
import time
from datetime import datetime, timedelta

# Third-party
import yfinance as yf
import torch
import numpy as np

# Django
from django.core.cache import cache
from django.db import models
from django.db.models import Q

# Project imports
from authentication.models import User
from news.utils import analyze_sentiment
```

---

## JavaScript/React Coding Standards

### Linting and Formatting Tools

| Tool | Purpose | Configuration |
|------|---------|---------------|
| **ESLint** | Code linting | Airbnb style guide |
| **Prettier** | Code formatting | Single quotes, trailing commas |

### Configuration Files

**.eslintrc.js:**

```javascript
module.exports = {
  extends: [
    'airbnb',
    'airbnb/hooks',
    'plugin:react/recommended',
    'plugin:import/recommended',
    'plugin:jsx-a11y/recommended',
  ],
  parser: '@babel/eslint-parser',
  parserOptions: {
    ecmaVersion: 2022,
    sourceType: 'module',
    ecmaFeatures: {
      jsx: true,
    },
  },
  env: {
    browser: true,
    es2022: true,
    node: true,
  },
  rules: {
    'react/prop-types': 'warn',
    'react/jsx-props-no-spreading': 'off',
    'react/function-component-definition': ['error', {
      namedComponents: 'arrow-function',
    }],
    'import/no-extraneous-dependencies': ['error', {
      devDependencies: ['**/*.test.js', '**/*.spec.js'],
    }],
    'jsx-a11y/label-has-associated-control': ['error', {
      required: {
        some: ['nesting', 'id'],
      },
    }],
  },
};
```

**.prettierrc:**

```json
{
  "singleQuote": true,
  "trailingComma": "es5",
  "tabWidth": 2,
  "semi": true,
  "printWidth": 88,
  "arrowParens": "always",
  "jsxSingleQuote": false,
  "bracketSpacing": true,
  "bracketSameLine": false
}
```

### JavaScript Naming Conventions

| Element | Convention | Example |
|---------|------------|---------|
| **Components** | PascalCase | `LoadingSpinner` |
| **Functions** | camelCase | `handleSubmit` |
| **Variables** | camelCase | `totalPredictions` |
| **Constants** | UPPER_SNAKE | `MAX_RETRIES` |
| **Hooks** | useCamelCase | `useAuth` |
| **Context** | PascalCase | `AuthContext` |

### JavaScript Code Examples

**Good:**

```javascript
// hooks/useAuth.js
import { useState, useEffect, useCallback, useMemo, useRef } from 'react';
import { useNavigate } from 'react-router-dom';
import apiClient, { setAuthToken, removeAuthToken } from '@/services/client';

const STORAGE_KEYS = {
  ACCESS_TOKEN: 'accessToken',
  REFRESH_TOKEN: 'refreshToken',
  USER: 'user',
  LAST_ACTIVITY: 'lastActivity',
};

const SESSION_TIMEOUT = 60 * 60 * 1000; // 60 minutes

export const useAuth = () => {
  const navigate = useNavigate();
  const [user, setUser] = useState(null);
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState(null);
  const [userTier, setUserTier] = useState('free');

  const refreshIntervalRef = useRef(null);
  const activityTimeoutRef = useRef(null);
  const isMountedRef = useRef(true);
  const refreshingRef = useRef(false);
  const initAuthCalled = useRef(false);

  const updateUser = useCallback((userData, remember = false) => {
    if (!userData) {
      setUser(null);
      setIsAuthenticated(false);
      setUserTier('free');
      localStorage.removeItem(STORAGE_KEYS.USER);
      sessionStorage.removeItem(STORAGE_KEYS.USER);
      return;
    }

    setUser(userData);
    setIsAuthenticated(true);
    setUserTier(userData.tier || 'free');

    const storage = remember ? localStorage : sessionStorage;
    storage.setItem(STORAGE_KEYS.USER, JSON.stringify(userData));
  }, []);

  const login = useCallback(async (username, password, rememberMe = false) => {
    setIsLoading(true);
    setError(null);

    try {
      const response = await apiClient.post('/auth/login/', { username, password });
      const { access, refresh, user: userData } = response.data.data || response.data;

      if (!access || !refresh) {
        throw new Error('Invalid login response');
      }

      const storage = rememberMe ? localStorage : sessionStorage;
      storage.setItem(STORAGE_KEYS.ACCESS_TOKEN, access);
      storage.setItem(STORAGE_KEYS.REFRESH_TOKEN, refresh);
      storage.setItem(STORAGE_KEYS.USER, JSON.stringify(userData));

      setAuthToken(access, rememberMe);
      updateUser(userData, rememberMe);
      updateLastActivity();
      startActivityMonitor();

      return { success: true, user: userData };
    } catch (err) {
      setError(err.message || 'Login failed');
      return { success: false, error: err.message };
    } finally {
      setIsLoading(false);
    }
  }, [updateUser, updateLastActivity, startActivityMonitor]);

  // ... rest of hook

  return useMemo(() => ({
    user,
    isAuthenticated,
    isLoading,
    error,
    userTier,
    login,
    // ... other functions
  }), [user, isAuthenticated, isLoading, error, userTier, login]);
};

export default useAuth;
```

**Bad:**

```javascript
// hooks/useAuth.js (bad example)
import { useState } from 'react'

export const useAuth = () => {
  const [user, setUser] = useState(null)
  const [isAuth, setIsAuth] = useState(false)
  const [loading, setLoading] = useState(true)
  
  const login = async (u, p, remember) => {
    try {
      const r = await apiClient.post('/auth/login/', { username: u, password: p })
      // ... poorly structured code
    } catch (e) {
      // ... inconsistent error handling
    }
  }
  
  return { user, isAuth, loading, login }
}
```

---

## Commit Message Guidelines

### Format

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Types

| Type | Description |
|------|-------------|
| **feat** | New feature |
| **fix** | Bug fix |
| **docs** | Documentation changes |
| **style** | Code style changes (formatting, missing semicolons) |
| **refactor** | Code refactoring |
| **perf** | Performance improvements |
| **test** | Adding or updating tests |
| **chore** | Maintenance tasks (dependencies, configs) |

### Examples

```
feat(stocks): add LSTM prediction endpoint

- Add LSTMPredictionView with symbol and news parameters
- Implement fallback to sentiment-only predictions
- Save predictions to prediction history

Closes #123
```

```
fix(auth): resolve CORS issue in production

- Add localhost:4173 to CORS_ALLOWED_ORIGINS
- Update middleware ordering for API key auth

Fixes #456
```

---

## PR Process

### Pull Request Template

```markdown
## Description
Brief description of the changes

## Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Testing
- [ ] Unit tests added/updated
- [ ] Integration tests added/updated
- [ ] Manual testing performed

## Checklist
- [ ] Code follows project style guidelines
- [ ] Self-review performed
- [ ] Documentation updated
- [ ] No new warnings or errors

## Related Issues
Closes #123
```

### Code Review Checklist

| Check | Description |
|-------|-------------|
| **Functionality** | Does the code work as expected? |
| **Style** | Does the code follow project style guidelines? |
| **Performance** | Are there any performance concerns? |
| **Security** | Are there any security vulnerabilities? |
| **Testing** | Are there tests for new functionality? |
| **Documentation** | Is the documentation updated? |

---

## Continuous Integration

### GitHub Actions Configuration

```yaml
# .github/workflows/ci.yml
name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint-python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install black flake8 isort
      - name: Check formatting (Black)
        run: black --check .
      - name: Run flake8
        run: flake8 .
      - name: Check imports (isort)
        run: isort --check-only .

  lint-javascript:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          cd frontend
          npm install
      - name: Run ESLint
        run: |
          cd frontend
          npm run lint
      - name: Check formatting (Prettier)
        run: |
          cd frontend
          npx prettier --check .
```

---

## Next Steps

- [7.4 Testing](7.4-Testing.md) – Testing strategy and implementation
- [7.5 Adding a New Feature](7.5-Adding-Features.md) – Feature development process
- [7.6 Debugging](7.6-Debugging.md) – Debugging techniques and tools